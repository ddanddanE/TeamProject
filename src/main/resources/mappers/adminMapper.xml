<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.admin">
	<insert id="insert">
		INSERT INTO member (userId, userPw, userName, phoneNum, email, sex, levelCount) VALUES (#{userId}, #{userPw}, #{userName},#{phoneNum}, #{email}, #{sex}, 1)
	</insert>
</mapper>